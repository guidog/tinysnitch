#!/usr/bin/env pypy3
import opensnitch.kprobe

opensnitch.kprobe.disable()
try:
    opensnitch.kprobe.enable()
    with open(opensnitch.kprobe.events_pipe_file, 'rb') as pipe:
        for line in pipe:
            print(line.rstrip())
except:
    raise
finally:
    opensnitch.kprobe.disable()
