#!/usr/bin/env bpftrace
/*
 * TODO the output of multiple tracepoints can be interleaved because
 * there isnt a lock around printf() and join()
 */

tracepoint:syscalls:sys_enter_execve
{
	printf("execve: %d ", pid);
	join(args->argv);
}
